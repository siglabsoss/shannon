#include "poppeak.hpp"

#include <iostream>
#include <boost/lexical_cast.hpp>

using namespace std;

namespace pop
{



	// cout << "size_t is " << sizeof(size_t) << endl;
	// cout << "uint8_t is " << sizeof(uint8_t) << endl;
	// cout << "uint16_t is " << sizeof(uint16_t) << endl;
	// cout << "PopTimestamp is " << sizeof(PopTimestamp) << endl;
	// cout << "popComplex is " << sizeof(popComplex) << endl;
	// cout << "data_point_t is " << sizeof(data_point_t) << endl;

	// cout << "size is " << sizeof(PopPeak) << endl;

	

//PopSymbol::PopSymbol(uint8_t s, double m, uint8_t fb, uint8_t c, uint16_t h, PopTimestamp &ts): symbol(s), magnitude(m), fbin(fb), channel(c), host(h), timestamp(ts)
//{
//
//}
//
//PopSymbol::~PopSymbol()
//{
//
//}
//
//void PopSymbol::debug_print()
//{
//	cout << "  symbol: " << (int)symbol << endl;
//	cout << "  magnitude: " << magnitude << endl;
//	cout << "  host: " << host << endl;
//	cout << "  time: " << boost::lexical_cast<string>(timestamp.get_full_secs()) << "   -   " << boost::lexical_cast<string>(timestamp.get_frac_secs()) << endl;
//}

}
